[build]
  base    = "exempleSite"
  command = "hugo --gc --minify -b $URL"
  publish = "public"

[build.environment]
  HUGO_VERSION   = "0.140.0"
  HUGO_ENV       = "production"
  HUGO_THEMESDIR = "../.."
  HUGO_THEME     = "repo"
  TZ             = "Europe/Paris"
